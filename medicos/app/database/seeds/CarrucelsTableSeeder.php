<?php

class CarrucelsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('carrucels')->truncate();

		$carrucels = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('carrucels')->insert($carrucels);
	}

}
